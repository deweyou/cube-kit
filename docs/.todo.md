# Memory TODO

- [ ] Decide whether old feature archives should be recreated, left deleted, or superseded by `docs/superpowers/`.
- [ ] Confirm the intended WeChat miniprogram timer interaction model before porting web behavior.
- [ ] Evaluate moving `@cubekit/scramble` browser usage into a Web Worker instead of relying on the main-thread shim.
- [ ] Fix pre-existing `pnpm check` formatting issues outside this memory initialization.
