# Memory TODO

- [ ] Decide whether old feature archives should be recreated, left deleted, or superseded by `docs/superpowers/`.
- [ ] Confirm the intended WeChat miniprogram timer interaction model before porting web behavior.
- [ ] Evaluate mobile worker lifecycle and queue pressure before increasing timer scramble prefetch depth.
- [ ] Add Playwright or equivalent E2E coverage against `apps/playground?seed=<n>` for stable scramble-core/image/solver smoke validation.
- [ ] Expose catalog-gated training bottom/front orientation in playground and user settings; persist the preference only after its product scope is confirmed.
- [ ] Diversify single-Case training states or define explicit canonical-only batch semantics so filtered batches above count `1` do not exhaust the uniqueness guard.
- [ ] Add browser smoke coverage for the playground Player tab canvas, progress controls, and static reference preview.
- [ ] Fix any remaining pre-existing root `pnpm check` lint/type issues in apps.
- [ ] Tune Square-1 player slash animation and final visual alignment against the `scramble-image` reference.
- [ ] Profile cached FTO coordinate-table initialization and constrained-state generation in the future wx-app Worker before exposing training ids in UI.
- [ ] Decide whether Redi/Dino belong in `packages/solver`, a separate package, or future non-WCA scope.
