# Memory State

- Last reviewed commit: `29a40ce`
- Iteration: `22`
- Last run: `mobile-timer-experience-pr`
- Covered areas: workspace layout, package CI, web timer workflow, scramble runtime, TNoodle baseline, TNoodle package split, scramble package coverage, scramble package docs, public cube-kit npm package, public cube-kit release workflow, cubegin clean-checkout static SVG release build, auxiliary/full solver package, scramble playground solver diagnostics, scramble docs app, scramble docs algorithm learning path, dependency licensing, README routing, web migration to TNoodle-compatible scramble packages, optional scramble-image isometric SVG renderers, cube paired six-face isometric preview, Megaminx paired 12-face isometric preview, Pyraminx companion-face isometric preview, Skewb paired six-face isometric preview, Clock/Square-1 isometric fallback, playground 2D/3D image-view switch, web timer persisted sessions, IndexedDB solve storage, result penalty actions, solve detail editing, Skewb Face auxiliary solver, cstimer-style staged 3x3 helpers, cstimer 3x3 General mask solver, Clock full solver migration, icons static SVG asset API, brand asset API, React animated Cubegin mark API, web timer brand/loading integration, mobile touch start flow, and web scramble worker prefetch
- Open risks: WeChat miniprogram is still a shell; root `pnpm check` may still report pre-existing wx-app, scramble-docs, root script, and test type-resolution issues outside this implementation; playground E2E remains a follow-up; scramble docs is educational content and not an official WCA scramble program; mobile worker lifecycle and queue pressure should be evaluated before increasing prefetch depth; 3x3 auxiliary solver scope rejects cube rotations and wide moves; 2x2 auxiliary helpers intentionally follow the URF coordinate move set; non-WCA cstimer full-puzzle solvers such as FTO/Redi/Dino remain outside the current WCA runtime scope
